
## 2026-06-23 商机管理系统搭建

### 完成工作
- 完整搭建「中睿智能商机管理系统」前后端项目
- **后端**: Node.js + Express + TypeScript + JSON文件数据库(避免原生模块编译问题)
- **前端**: React + TypeScript + Ant Design + Vite
- 功能: JWT登录认证、管理员/销售角色权限、商机CRUD、跟进记录、数据分析图表、用户管理
- 默认账号: admin / admin123

### 修复问题
- better-sqlite3 原生模块编译失败 → 换用JSON文件数据库方案
- OpportunityDetail.tsx 导入路径错误 (`../../utils/request` → `../utils/request`)
- 前端端口3000，后端端口3001，均已正常运行

### 线上公网穿透
- 使用 localtunnel 将后端(3001端口)穿透到公网: https://zhongrui-crm.loca.lt
- 前端构建产物已嵌入后端public目录，统一由后端提供服务
- 需要保持本地电脑开机和后端运行才能维持公网访问
- 后端服务多次崩溃(500错误)，原因是进程异常退出，需手动重启(npx tsx watch src/index.ts)

### 局域网部署完成
- 局域网 IP: 192.168.49.67:3001
- 后端绑定 0.0.0.0，局域网任意设备可访问
- 前端构建时 API 指向 http://192.168.49.67:3001/api
- 前后端统一由 3001 端口提供服务（静态文件 + API）
- 启动脚本: start-lan.bat
- 默认账号: admin / admin123

### Render.com 部署完成 (11:26)
- 用户按指引在 Render.com 使用 Free 套餐部署成功
- 在线地址: https://zhongrui-crm.onrender.com
- 已验证: 前端页面正常加载，后端 API (/api/health) 返回 OK
- GitHub 仓库: github.com/jiang-jie66/zhongrui-crm

### 删除销售账号功能 (13:37)
- 后端: jsonDb.ts 添加 deleteUserById 函数，删除用户同时将关联商机的 sales_id 置空
- 后端: auth.ts 添加 DELETE /:id 路由（管理员专用，不可删除自己和管理员）
- 前端: UserManage.tsx 操作列添加"删除"按钮，带 Popconfirm 二次确认
- 部署方式: 因 github.com 443 端口被墙，改用 GitHub Content API (api.github.com) 直接推送文件
- 已构建前端并推送到 GitHub，Render 自动部署生效
- 在线地址: https://zhongrui-crm.onrender.com

### 完整响应式适配 (13:52)
- 断点: 移动端 <768px | 平板 768-1024px | PC >1024px
- 创建 src/index.css 全局响应式样式，含移动端/平板/PC三档媒体查询
- App.tsx: 侧边栏移动端切换为 Drawer 抽屉菜单（汉堡按钮），PC端可折叠 Sider
- Login.tsx: 卡片宽度自适应，移动端全宽，渐变背景
- Dashboard.tsx: 统计卡片响应式 Grid (移动端1列/平板2-3列/PC端5列)
- OpportunityList.tsx: 搜索栏移动端纵向排列，表格列响应式隐藏（responsive属性），分页简化
- OpportunityDetail.tsx: Descriptions 移动端1列/PC端2列，弹窗宽度自适应
- Analytics.tsx: 图表高度自适应，饼图外半径动态调整，统计表格移动端纵向排列
- UserManage.tsx + OperationLogs.tsx: 表格/按钮/弹窗全面响应式
- 所有 Modal 移动端全宽显示 (maxWidth: calc(100vw - 24px))
- 所有 Select 添加 getPopupContainer 防止移动端弹出层定位异常
- 已构建推送到 GitHub，等待 Render 自动部署

### 四项修复 + Logo添加 (14:08)
- **问题1**: 商机列表移动端表格显示不全（只能看到公司名）→ scroll.x 从600→800
- **问题2**: 操作日志移动端列宽不足文字重叠 → scroll.x 从350→550，detail列设置minWidth:140
- **问题3**: 添加公司Logo → 从 E:\ZORAX\ZORAX\中睿LOGO\ 复制到 client/public/ 和 server/public/，Login页用img替换圆形"中睿"文字，App.tsx Header添加logo图标
- **问题4**: 登录页底部提示改为"升级中国智造！"（蓝色加粗，letterSpacing:2）
- 已推送 GitHub，用户需在Render Dashboard手动触发 "Manual Deploy" → "Deploy latest commit"
